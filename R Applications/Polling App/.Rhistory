setwd("D:/DC Universe/Ucsc/Third Year/ENH 3201 Industrial Placements/ECL Project/R Project/Polling")
Sys.getenv("api_key")
Sys.getenv("db_url")
Sys.setenv("api_key = AIzaSyApO_MpCV_iCAl4BipzOVKHHDhLnVrkc9w")
Sys.setenv(api_key = "AIzaSyApO_MpCV_iCAl4BipzOVKHHDhLnVrkc9w")
Sys.getenv("api_key")
Sys.setenv(db_url = "https://pandupoll-default-rtdb.firebaseio.com/")
Sys.getenv("db_url")
install.packages("flexdashboard")
install.packages("tidyverse")
install.packages("gt")
install.packages("htmltools")
install.packages("viridis")
install.packages(c("backports", "broom", "cli", "corrplot", "cpp11", "crayon", "crosstalk", "digest", "DT", "fs", "generics", "glue", "lmtest", "memoise", "microbenchmark", "nloptr", "pkgbuild", "RcppArmadillo", "rlang", "RSQLite", "stringi", "tibble", "tseries", "V8", "withr", "xfun"))
install.packages("highcharter")
Sys.getenv("api_key")
Sys.getenv("db_url")
download_df <- function(projectURL, fileName) {
rbindlist(
download(projectURL = projectURL, fileName = paste0("main/", fileName))
)
}
Sys.getenv("db_url")
rbindlist(
download(projectURL = projectURL, fileName = paste0("main/", fileName))
)
rbindlist(
download(projectURL = projectURL, fileName = paste0("main/", fileName))
)
upload_row <- function(x = mtcars, projectURL = Sys.getenv("db_url"), fileName) {
upload(x = x, projectURL = projectURL, directory = paste0("main/", fileName))
}
#download_df <- function(projectURL, fileName) {
rbindlist(
download(projectURL = projectURL, fileName = paste0("main/", fileName))
)
}
#download_df <- function(projectURL, fileName) {
rbindlist(
download(projectURL = projectURL, fileName = paste0("main/", fileName))
)
download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#
upload_row <- function(x = mtcars, projectURL = Sys.getenv("db_url"), fileName) {
upload(x = x, projectURL = projectURL, directory = paste0("main/", fileName))
}
#{
upload(x = mtcars, projectURL =  Sys.getenv("db_url"))
#{
#download_df <- function(projectURL, fileName) {
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#}
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#}
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#}
#download_df <- function(projectURL, fileName) {
#  rbindlist(
#    download(projectURL = projectURL, fileName = paste0("main/", fileName))
#  )
#}
upload(x = mtcars, projectURL =  Sys.getenv("db_url"))
View(download_df)
View(download_df)
View(download_df)
View(upload_row)
Sys.getenv("db_url")
upload(x = mtcars, projectURL = Sys.getenv("db_url"))
upload(x = mtcars, projectURL = Sys.getenv("db_url"))
shiny::runApp()
runApp()
library(fireData)
upload(x = mtcars, projectURL = Sys.getenv("db_url"))
